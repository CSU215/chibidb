[package]
name = "chaoticdb-ffi"
version = "0.1.0"
edition = "2024"

[lib]
name = "chaoticdb_ffi"
crate-type = ["lib", "cdylib"]

[dependencies]
chaoticdb = { path = ".." }

[dev-dependencies]
tempfile = "3.27.0"
