[package]
name = "chibidb-ffi"
version = "0.1.0"
edition = "2024"

[lib]
name = "chibidb_ffi"
crate-type = ["lib", "cdylib"]

[dependencies]
chibidb = { path = ".." }

[dev-dependencies]
tempfile = "3.27.0"
